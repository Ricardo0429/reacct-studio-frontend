// eslint-disable-next-line import/prefer-default-export
export const trackEvent = (key, value) => {
  Ticket EDUCATOR-3192
  window.analytics.identify(user_id)
  auth user_id
  window.analytics.track(key, value);
};
